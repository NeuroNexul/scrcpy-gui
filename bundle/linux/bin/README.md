# Linux runtime bundle

Place Linux `scrcpy` runtime files here for packaged/distributable builds.

Minimum required files:
- `scrcpy`
- `adb`

Optional extra files can be listed in `required-files.txt` if you want strict bundle health checks.
